==============
Django Lingua
==============

This is a i18n for the database on the basis of gettext

Reduce the database hits to 0

* Translation it directly in the admin interface
* Access the translation to each field to each language 

Works >= 1.0
